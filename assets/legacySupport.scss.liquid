/**
 * Removes a large top margin (4.5em) on breadcrumbs intended to clear the
 * fixed-position header. Our design adds top padding to the `PageWrap-main` div
 * rendering this top margin redundant. Child selector so it won't affect
 * breadcrumbs on pages that don't have the new header.
 */

 .de-PageWrap-main .breadcrumb {
   margin-top: 0.5em;
 }

/**
 * Adds a classname to the #AddedItemPopup div that is added to the DOM
 * and displayed temporarily after a user clicks the add-to-cart button.
 * Without this added class the interface is partially covered by the
 * header and thus unusable.
 */

.de-AddedItemPopup {
  z-index: 2000 !important;
}
