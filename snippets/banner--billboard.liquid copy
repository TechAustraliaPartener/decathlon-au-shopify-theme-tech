<div class="banner banner--billboard{{ banner_adtl_classes }}"{% if banner_bg_image %} data-background-image="{{ banner_bg_image }}"{% endif %}>
  <div class="wrapper">
    <div class="banner-content">
      {%- if banner_intro -%}
        <p class="banner-intro">{{ banner_intro }}</p>
      {%- endif -%}

      {%- if banner_title -%}
        <h1 class="banner-title{{ banner_title_adtl_classes }}">{{ banner_title }}</h1>
      {%- endif -%}

      {%- if banner_subtitle -%}
        <p class="banner-subtitle{% unless button_url %} u-marginBottom0x{% endunless %}">{{ banner_subtitle }}</p>
      {%- endif -%}

      {%- if button_url -%}
        {%- include 'elements-button' -%}
      {%- endif -%}
    </div>
  </div>
</div>
