<!DOCTYPE html>
<html lang="{{ locale }}" dir="{{ direction }}" class="{{ checkout_html_classes }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, height=device-height, minimum-scale=1.0, user-scalable=0">
    <meta name="referrer" content="origin">

    <title>{{ page_title }}</title>

    {{ content_for_header }}

    {{ checkout_stylesheets }}
    {{ 'patterns-toolkit.css' | asset_url | stylesheet_tag }}
    {{ checkout_scripts }}

  </head>
  <body>
    {{ skip_to_content_link }}

    {% include 'google-tag-manager' %}

    <div class="banner" data-header>
      <div class="wrap">
        {{ content_for_logo }}
      </div>
  {% comment %}
    script for Minubo
  {% endcomment %}
  <script>
    window.dataLayer = window.dataLayer || [];
    dataLayer.push({
      'random': Math.random().toString(36).substr(2,9),
      'timestamp': new Date().getTime()
    });
  </script>
    </div>

    {{ order_summary_toggle }}

    <div class="content" data-content>
      <div class="wrap">
        <div class="main" role="main">
          <div class="main__header">
            {{ content_for_logo }}
            {{ breadcrumb }}
            {{ alternative_payment_methods }}
          </div>
          <div class="main__content">

            {% comment %}
              The Ship & Pickup buttons start hidden.
              The checkout JS (scripts/checkout/index.js)
              will show and add UI functionality.
            {% endcomment %}
            <button class="btn js-ship-toggle-btn de-u-hidden">
              <span class="btn__content">Ship</span>
            </button>
            <button class="btn js-pickup-toggle-btn de-u-hidden">
              <span class="btn__content">Pickup</span>
            </button>

            {{ content_for_layout }}
          </div>
          <div class="main__footer">
            {{ content_for_footer }}
          </div>
        </div>
        <div class="sidebar" role="complementary">
          <div class="sidebar__header">
            {{ content_for_logo }}
          </div>
          <div class="sidebar__content">
            {{ content_for_order_summary }}
          </div>
        </div>
      </div>
    </div>
    {{ 'checkout.js' | asset_url | script_tag }}
    {{ tracking_code }}

    {{ 'checkout.js' | asset_url | script_tag }}
  </body>
</html>
